{% extends 'base.html.twig' %}

{% block body %}
    <div class="jumbotron">
        <h1>Tout les évenements</h1>
    </div>

    {% for event in events %}

    <p><a href="{{ path('eventById', {id_event:event.id_event}) }}">{{ event.nom_event }}</a>
       <a href="{{ path('deleteShop', {id_event: event.id_event}) }}">
           Supprimer
       </a>
   </p>

    {% endfor %}

{% endblock %}